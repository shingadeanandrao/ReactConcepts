import React from "react";

//create a new Context

// Context is used for passing properties directly from parent comp to any component directly

const myContext = React.createContext();

export default myContext